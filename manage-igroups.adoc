---
sidebar: sidebar
permalink: manage-igroups.html
keywords: igroup, manage igroups, manage client access, delete igroup
summary: "Manage igroups for an FSx for ONTAP file system in NetApp Workload Factory." 
---
= Manage block storage for a file system in NetApp Workload Factory
:icons: font
:imagesdir: ./media/

[.lead]
Use NetApp Workload Factory to simplify managing your block storage resources, including igroups and block devices, and control client access for FSx for ONTAP block devices to provide optimal performance and minimize costs. 

.About this task
From the Workload Factory console, you can manage initiator groups (igroups) to control client access to block devices, and manage block devices to view details, increase capacity, manage client access, archive data for inactive block devices, and delete block devices.

Refer to the igroup diagram to check for the following: 

* Client connections
* Block device status
* Node-client relationship
* Potential incorrect client connections

.Before you begin
* You must associate a link to manage igroups. link:https://docs.netapp.com/us-en/workload-fsx-ontap/create-link.html[Learn how to associate an existing link or to create and associate a new link]. After you associate the link, return to this operation.
* You must have an existing igroup or block device to view and manage.

== Manage igroups
You can view igroup details, manage client access, and delete igroups.

=== Manage client access for an igroup
You can manage client access for an existing igroup at any time.
 
.Steps
. Log in using one of the link:https://docs.netapp.com/us-en/workload-setup-admin/console-experiences.html[console experiences^].
. Select the menu image:workloads-menu-icon.png[The hamburger menu icon is used to navigate to workloads like storage, eda, ai, databases, vmware, and administration.] and then select *Storage*.
. From the Storage menu, select *FSx for ONTAP*.
. From *FSx for ONTAP*, select the *Block devices* tab. 
. Select the resource type *Initiator groups (igroups)* to view existing igroups.
. Go to the actions menu for the block device and select *Manage client access*.
. Review the client access details displayed for the igroup.
. To make changes to client access, select *Edit client access*.
. In the *Edit client access* dialog, you can edit the following:

* *igroup name*
* *igroup description*
* *Storage VM name*
* *Block device name*
* *Operating system type*
* *Host initiators*
. Select *Apply*.

=== Delete an igroup 
You can delete an existing igroup when it is no longer needed.    

.Steps
. Log in using one of the link:https://docs.netapp.com/us-en/workload-setup-admin/console-experiences.html[console experiences^].
. Select the menu image:workloads-menu-icon.png[The hamburger menu icon is used to navigate to workloads like storage, eda, ai, databases, vmware, and administration.] and then select *Storage*.
. From the Storage menu, select *FSx for ONTAP*.
. From *FSx for ONTAP*, select the *Block devices* tab. 
. Select the resource type *Initiator groups (igroups)* to view existing igroups.
. Navigate to the actions menu for the block device and then select *Delete initiator group*.
. In the Delete initiator group (igroup) dialog, type "delete" to confirm that you want to delete the igroup, and then select *Delete*.

== Manage block devices for an igroup
Block devices, or LUNs (logical unit numbers), are volumes that contain file systems in a SAN environment and can be accessed by hosts over a network.

You can manage block devices for FSx for ONTAP file systems that use the iSCSI protocol.

=== View block device details
View details for an existing block device at any time.

.Steps  
. Log in using one of the link:https://docs.netapp.com/us-en/workload-setup-admin/console-experiences.html[console experiences^].
. Select the menu image:workloads-menu-icon.png[The hamburger menu icon is used to navigate to workloads like storage, eda, ai, databases, vmware, and administration.] and then select *Storage*.
. From the Storage menu, select *FSx for ONTAP*.
. From *FSx for ONTAP*, select the *Block devices* tab. 
. Navigate to the actions menu for block device and then select *View details*.
+ 
General details, consumption, access, and protection information for the block device are displayed.

=== Increase the capacity of a block device
Increase the capacity of an existing block device at any time.    

.Steps
. Log in using one of the link:https://docs.netapp.com/us-en/workload-setup-admin/console-experiences.html[console experiences^].
. Select the menu image:workloads-menu-icon.png[The hamburger menu icon is used to navigate to workloads like storage, eda, ai, databases, vmware, and administration.] and then select *Storage*.
. From the Storage menu, select *FSx for ONTAP*.
. From *FSx for ONTAP*, select the *Block devices* tab. 
. Navigate to the actions menu for the block device and then select *Increase capacity*.
. Enter the new capacity for the block device and select the unit. 
. Select *Increase* to apply the changes.

NOTE: After you increase the size of the block device, follow the procedure provided by your host operating system to discover the new size of the block device and expand the file system on it.

=== Manage client access for a block device
You can manage client access for an existing block device at any time by creating igroups, and adding or removing block devices and host initiators.

.Steps
. Log in using one of the link:https://docs.netapp.com/us-en/workload-setup-admin/console-experiences.html[console experiences^].
. Select the menu image:workloads-menu-icon.png[The hamburger menu icon is used to navigate to workloads like storage, eda, ai, databases, vmware, and administration.] and then select *Storage*.
. From the Storage menu, select *FSx for ONTAP*.
. From *FSx for ONTAP*, select the *Block devices* tab. 
. Navigate to the actions menu for the block device and then select *Manage client access*.
. If no igroup exists, create a new igroup by selecting *Create igroup*, and then do the following: 
.. *Block device name*: Enter a block device name. You can select multiple block devices to associate with the igroup.
.. *Operating system type*: Select the operating system type.
.. *igroup name*: Enter an igroup name.
.. *igroup description*: Optionally, enter an igroup description.
.. *Host initiators*: Enter one or more host initiators. These initiators must follow iSCSI qualified (IQN) format.
.. Select *Create*.
. If an igroup already exists, select *Edit client access* to add or remove block devices and host initiators from the igroup and then select *Apply*.

=== Archive the data of an inactive block device
Block devices that are no longer mapped to a client or unused for seven consecutive days are classified as inactive block devices. You can archive the data of an inactive block device to the capacity pool tier to reclaim SSD capacity.

.Steps
. Log in using one of the link:https://docs.netapp.com/us-en/workload-setup-admin/console-experiences.html[console experiences^].
. Select the menu image:workloads-menu-icon.png[The hamburger menu icon is used to navigate to workloads like storage, eda, ai, databases, vmware, and administration.] and then select *Storage*.
. From the Storage menu, select *FSx for ONTAP*.
. From *FSx for ONTAP*, select the *Block devices* tab. 
. Under Orphan devices, select *View and reclaim capacity*.
. On the Reclaim space for unused block devices screen, select one or more block devices to archive the data and reclaim the capacity.
. Select *Archive*. 

=== Delete a block device
Block devices that are no longer mapped to a client or unused for seven consecutive days are classified as inactive block devices. This operation unmaps and deletes the selected block device. If the host FlexVol volume doesn't contain any block devices, Workload Factory also deletes it.

.Steps
. Log in using one of the link:https://docs.netapp.com/us-en/workload-setup-admin/console-experiences.html[console experiences^].
. Select the menu image:workloads-menu-icon.png[The hamburger menu icon is used to navigate to workloads like storage, eda, ai, databases, vmware, and administration.] and then select *Storage*.
. From the Storage menu, select *FSx for ONTAP*.
. From *FSx for ONTAP*, select the *Block devices* tab. 
. Under Orphan devices, select *View and reclaim capacity*.
. On the Reclaim space for unused block devices screen, select one or more block devices to archive the data and reclaim the capacity.
. Select *Delete*. 

== Related information

link:https://docs.netapp.com/us-en/workload-fsx-ontap/create-igroup.html[Create an igroup for an FSx for ONTAP file system]
